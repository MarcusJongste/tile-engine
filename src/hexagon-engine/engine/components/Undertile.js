

export default function Undertile({ textures }) {
    return (
        <div className="undertile">
            {textures/*.length > 0 ? createTextures(textures, id, action, shape) : ""*/}
        </div>
    );
}